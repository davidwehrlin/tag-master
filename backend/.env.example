# NOTE: Docker Compose variable substitution
# ----------------------------------------
# Docker Compose (the `docker compose` command) automatically reads a project-level
# `.env` file located at the repository root for variable substitution. See the
# repository `./.env` for Docker/Postgres configuration (POSTGRES_USER, POSTGRES_PASSWORD,
# POSTGRES_DB). Use this `backend/.env.example` as the template for the application's
# runtime environment (copy to `backend/.env` when running the app locally).

# Database Configuration
# Individual Postgres settings (used by docker-compose and local runs)
POSTGRES_USER=your-test-user
POSTGRES_PASSWORD=your-test-password
POSTGRES_DB=your-test-database

# Database URL (fallback if you prefer a single connection string)
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# JWT Configuration
# Generate a secure secret key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-change-in-production-minimum-32-characters-recommended
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
# Maximum requests per minute per authenticated user
RATE_LIMIT_PER_MINUTE=50

# Monitoring (optional)
# Webhook URL for monitoring alerts (Slack, Discord, etc.)
MONITORING_WEBHOOK_URL=

# Environment
# Options: development, staging, production
ENVIRONMENT=development

# Logging
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
