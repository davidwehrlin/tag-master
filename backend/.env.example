# Database Configuration
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://tagmaster:dev_password@localhost:5432/tagmaster_dev

# JWT Configuration
# Generate a secure secret key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-change-in-production-minimum-32-characters-recommended
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
# Maximum requests per minute per authenticated user
RATE_LIMIT_PER_MINUTE=50

# Monitoring (optional)
# Webhook URL for monitoring alerts (Slack, Discord, etc.)
MONITORING_WEBHOOK_URL=

# Environment
# Options: development, staging, production
ENVIRONMENT=development

# Logging
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
